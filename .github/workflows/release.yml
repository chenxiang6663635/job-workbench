name: Release

# 打 tag 即产出安装包并挂到该 Release。
#
# 为什么要有这个 workflow（不是"顺手自动化一下"）：
# v0.1.1 的安装包是 batch B/C 之前构建的，之后主干还换过图标——下载页上那份 exe
# 已经不代表当前代码，而且**没有任何机制会发现这件事**。让 tag 触发构建之后，
# 资产与 tag 同源，「安装包与代码脱节」在结构上不再可能发生。
#
# 三道闸都不通过就拒绝发布，且每条都给出可照做的修法：
#   1. tag 与 web/electron/package.json 的版本号必须一致（否则资产名会说谎）
#   2. CHANGELOG.md 里必须有该版本的条目（否则等于发布了没人知道的东西）
#   3. 必须真的产出 .exe（否则不许静默成功）
#
# 拆成两个 job 的原因：**权限不能下沉到 step，只能下沉到 job**。构建链里跑了
# npm ci（任意依赖的 postinstall）与 pip install，若让写权限覆盖整条链，任何一个
# 被投毒的依赖都可能拿到可写 token。所以构建只给读权限，另一个 job 才拿写权限。
on:
  push:
    tags:
      - "v*"
  # 手动触发用于演练：dry_run=true 只构建 + 校验 + 存成 workflow artifact，不碰 Release。
  # 这是唯一能在不打 tag 的情况下验证这条链路的方式。
  workflow_dispatch:
    inputs:
      dry_run:
        description: "只构建与校验，不创建 Release"
        type: boolean
        default: true
      tag:
        description: "演练用：模拟的 tag（如 v26.9.0）——校验 tag 比对与 CHANGELOG 段；真实发布（tag push）不需要填"
        type: string
        default: ""

# 默认最小权限。publish job 单独提权。
permissions:
  contents: read

# 同 ref 的发布串行：并发的两次运行会双双判定「Release 不存在」然后抢着创建。
# cancel-in-progress=false —— 已经开始的发布不该被后一次打断在半路。
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    name: 构建 Windows 安装包
    runs-on: windows-latest
    timeout-minutes: 60
    permissions:
      contents: read
    outputs:
      version: ${{ steps.version.outputs.version }}
      asset: ${{ steps.artifact.outputs.asset }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          # 默认会把 GITHUB_TOKEN 写进 .git/config 的 extraheader，之后 npm/pip
          # 拉的任何代码都能拿它直接 push。构建链不需要凭证，一律不带。
          persist-credentials: false

      # 先钉解释器再跑闸：两道闸都调 `jobws`（release_assist）——用 runner 自带的
      # python 等于把发布判据押在一个未钉版的解释器上（第二轨 MINOR-6）。
      - name: 安装 Python 3.12
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.12"

      # 版本号唯一来源是 web/electron/package.json（CONTRIBUTING 的规定）。
      # tag 与它不一致时直接停下：资产名 job-workbench-setup-<ver>-win64.exe 里的 ver
      # 取自 package.json，若两者不同，用户下载到的东西名字与实际版本就对不上。
      #
      # 月粒度 CalVer 体系（2026-09-24）：tag（v26.9.0）与 package.json（26.9.0）
      # 是**同一个号**，判定走唯一实现 tools/release_assist.py 的
      # version_matches_tag（逐字相等），本地预检与 CI 同源，不内联拼串比较。
      - name: 读取版本号并校验 tag 一致
        id: version
        shell: pwsh
        env:
          TAG_INPUT: ${{ inputs.tag }}
        run: |
          $ErrorActionPreference = "Stop"
          $version = (Get-Content web/electron/package.json -Raw -Encoding UTF8 | ConvertFrom-Json).version
          if (-not $version) { throw "读不到 web/electron/package.json 的 version" }
          Write-Host "package.json version = $version"
          "version=$version" >> $env:GITHUB_OUTPUT

          # 用 $env:GITHUB_REF 而不是把 ${{ github.ref }} 拼进脚本：外部可控的值
          # 经 shell 文本传递等于给它一次解释机会（本仓库 CONTRIBUTING 有此规定）。
          # 演练路径（workflow_dispatch 的 tag 输入）同样经 env 传入，不拼字符串。
          if ($env:GITHUB_REF -like "refs/tags/*") {
            $tag = $env:GITHUB_REF -replace "^refs/tags/", ""
            Write-Host "tag = $tag"
            python tools/jobws.py release check --tag $tag
            if ($LASTEXITCODE -ne 0) {
              throw "release check 失败（exit $LASTEXITCODE）——tag 与版本不一致或 CHANGELOG 缺段，按上文提示修"
            }
          } elseif ($env:TAG_INPUT) {
            Write-Host "演练 tag = $($env:TAG_INPUT)（workflow_dispatch 输入，仅校验不发布）"
            python tools/jobws.py release check --tag $env:TAG_INPUT
            if ($LASTEXITCODE -ne 0) {
              throw "release check 失败（exit $LASTEXITCODE）——演练 tag 与版本不一致或 CHANGELOG 缺段，按上文提示修"
            }
          } else {
            # 审计 P0-6：不带 tag 的手动演练走不下去——CHANGELOG 段名是发布号，
            # 没有演练 tag 就没有可校验的段，继续跑只会在下面的抽段步骤 exit 1。
            # 快速失败并把填法说清楚，别让人对着半截日志猜。
            Write-Error "workflow_dispatch 演练必须填写 tag 输入（如 v26.9.0，需与 CHANGELOG 的版本段一致）；不填只会在后面的 CHANGELOG 抽段步骤失败"
            exit 1
          }

      # CHANGELOG 是「使用者可见变更」的唯一台账（CONTRIBUTING 的规定）。缺条目就拒绝
      # 发布——这正是本批次要修的那类漂移：v0.1.1 之后 5 个 feat 一条都没记。
      # 抽取与校验的**唯一实现**在 tools/release_assist.py：本地预检
      # `jobws release check --tag vX`（打 tag 前跑）与这里同源；顺带修掉了旧内联
      # pwsh 里「$a[0..-1] 反向取值」那类只在边界触发的坑（v0.2.2 演练记录在案）。
      - name: 校验 CHANGELOG 有该版本条目并抽出发布说明
        shell: pwsh
        env:
          VERSION: ${{ steps.version.outputs.version }}
          TAG_INPUT: ${{ inputs.tag }}
        run: |
          $ErrorActionPreference = "Stop"
          # CHANGELOG 段名 = 版本号（如 26.9.0）——
          # 有 tag（真实 push 或演练输入）时带 --tag 让 release check 用 tag 的号查段。
          $tagValue = ""
          if ($env:GITHUB_REF -like "refs/tags/*") {
            $tagValue = $env:GITHUB_REF -replace "^refs/tags/", ""
          } elseif ($env:TAG_INPUT) {
            $tagValue = $env:TAG_INPUT
          }
          $checkArgs = @("release", "check", "--version", $env:VERSION)
          if ($tagValue) { $checkArgs += @("--tag", $tagValue) }
          # 放在工作区根而不是 RUNNER_TEMP：这个文件要随 artifact 传到 publish job，
          # 两个路径必须同源同层级，否则 artifact 内部路径不可预测。
          $checkArgs += @("--notes-out", "release-notes.md")
          python tools/jobws.py @checkArgs
          if ($LASTEXITCODE -ne 0) { throw "release check 失败（exit $LASTEXITCODE）——按上文提示修 CHANGELOG 后重跑" }

      # PyInstaller 不在 requirements.txt 里（它只是构建期依赖，不该打给终端用户）。
      # 上限按本仓库既有惯例（fastapi<0.142 那样）钉到主版本，不钉死小版本：
      # 具体小版本与 Python 基线、runner 镜像的兼容性耦合，钉死了反而会在 runner
      # 上装不上。
      # 解析到的实际版本由下一步打印进日志——不可复现时至少有据可查。
      # 注意不要在探测清单里加 filelock：它是仓内模块，不是 pip 依赖。
      - name: 安装后端依赖与 PyInstaller
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          python -m pip install --upgrade pip
          pip install -r web/backend/requirements.txt "pyinstaller<7"
          if ($LASTEXITCODE -ne 0) { throw "依赖安装失败（exit $LASTEXITCODE）" }

      - name: 安装 Node 20
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
        with:
          node-version: "20"

      # 记录工具链实际版本，便于日后复现或对比
      - name: 记录工具链版本
        shell: pwsh
        run: |
          python --version
          python -m PyInstaller --version
          node --version
          npm --version

      # 两个 package-lock.json 都在仓库里，用 npm ci 保证与锁文件一致
      - name: 安装前端与 electron 依赖
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          npm ci --prefix web/frontend
          if ($LASTEXITCODE -ne 0) { throw "前端依赖安装失败（exit $LASTEXITCODE）" }
          npm ci --prefix web/electron
          if ($LASTEXITCODE -ne 0) { throw "electron 依赖安装失败（exit $LASTEXITCODE）" }

      # 走与本地完全相同的那条链路：build_desktop.ps1 → build_backend_exe.ps1（前端 dist →
      # PyInstaller onedir）→ electron-builder NSIS。显式把解释器传给 -Py，避免脚本内的
      # 探测在这台机器上落到别的 Python。
      # runner 具备管理员权限，历史上本地踩过的 winCodeSign 符号链接解压问题在这里不会出现。
      - name: 构建安装包
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $py = (Get-Command python).Source
          Write-Host "使用解释器: $py"
          & powershell -ExecutionPolicy Bypass -File scripts/build_desktop.ps1 -Py "$py"
          if ($LASTEXITCODE -ne 0) { throw "打包失败（exit $LASTEXITCODE）" }

      # 打包后端冒烟：**构建成功 ≠ 能启动，也 ≠ 能用**。v0.2.2 的教训——tools/ 没进
      # PyInstaller 依赖图，`import imaplib` 在启动路径上失败，后端一启动就崩；而此前
      # 这一步只构建不运行产物，CI 全绿、缺陷一路跟到发布前。脚本做三件事：启动 +
      # `/api/system/check`（真执行 tools 代码）+ 打包资源自检（template/ 必须随包）——
      # 后两条是独立审查抓到的盲区。反向验证过（缺资源的旧产物跑它必红），不是空转。
      - name: 打包后端冒烟（能启动 + tools 端点 + 资源自检）
        shell: pwsh
        run: |
          python scripts/smoke_backend_exe.py web/backend/dist/job-workbench-backend/job-workbench-backend.exe
          if ($LASTEXITCODE -ne 0) { throw "打包后端冒烟失败（exit $LASTEXITCODE）——产物可能缺模块/资源，不要按绿发布" }

      - name: 定位产物与更新元数据
        id: artifact
        shell: pwsh
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          $ErrorActionPreference = "Stop"
          $dir = "web/electron/release"
          $found = Get-ChildItem $dir -Filter *.exe -ErrorAction SilentlyContinue
          if (-not $found) {
            # 不许静默成功：没产物就是失败，并说清去哪找线索
            throw "构建结束但 $dir 下没有 .exe。看上一步输出里 electron-builder 的报错。"
          }
          $found | ForEach-Object { Write-Host ("找到产物: {0}  ({1:N1} MB)" -f $_.Name, ($_.Length / 1MB)) }
          if ($found.Count -gt 1) {
            # 不猜该发哪个。本地全量构建出来恰好 1 个 exe，所以这里若被触发
            # 说明产出形态变了，需要人看一眼再决定。
            throw "$dir 下有多个 .exe，无法判断该发布哪个：$($found.Name -join ', ')"
          }

          # 产物名由 build.artifactName 决定（job-workbench-setup-<ver>-win64.exe）。
          # 这里只**校验**名字，**不再改名**——latest.yml 里记录的下载路径必须与资产名
          # 逐字一致，改名就是让两者脱钩：客户端拿到元数据后按里面的文件名下载会 404。
          $expected = "job-workbench-setup-$($env:VERSION)-win64.exe"
          if ($found[0].Name -ne $expected) {
            throw "产物名与预期不符：实际 $($found[0].Name)，预期 $expected（见 build.artifactName）。改名会让 latest.yml 里的路径对不上。"
          }

          $asset = $expected
          Copy-Item $found[0].FullName $asset -Force

          # 自动更新元数据：electron-updater 靠它判断有没有新版、该下载哪个文件。
          # electron-builder 只在配置了 build.publish 时才产出它，所以缺了就是配置退化。
          $meta = Join-Path $dir "latest.yml"
          if (-not (Test-Path $meta)) {
            throw "没有生成 latest.yml —— 自动更新的元数据缺失。检查 build.publish 配置。"
          }
          Copy-Item $meta "latest.yml" -Force

          # 差量更新用的块映射：没有它客户端会退化成全量下载（不失败），有则省流量
          $blockmap = Get-ChildItem $dir -Filter "*.blockmap" -ErrorAction SilentlyContinue
          if ($blockmap) {
            Copy-Item $blockmap[0].FullName "$asset.blockmap" -Force
            Write-Host "已带上块映射: $asset.blockmap"
          } else {
            Write-Host "没有 .blockmap，客户端将走全量下载（不视为失败）"
          }

          Write-Host "资产名: $asset"
          "asset=$asset" >> $env:GITHUB_OUTPUT

      # 发布公告的两件套（2026-09-24 发布调研结论）：无签名软件的**预期管理**
      # （SmartScreen 处理说明，微软官方口径：未签名拦截页更严重、每版本信誉归零，
      # 公告里写明处理方法是最便宜的补偿）与 **SHA256 校验清单**（没有代码签名的
      # 情况下，用户核对哈希是"可信发布"的最低成本替代）。
      - name: 生成 SHA256 清单并附加安装说明
        shell: pwsh
        env:
          ASSET: ${{ steps.artifact.outputs.asset }}
        run: |
          $ErrorActionPreference = "Stop"
          if (-not (Test-Path "latest.yml")) { throw "latest.yml 不存在——自动更新元数据缺失，不要按绿发布" }
          $hashLines = Get-FileHash -Algorithm SHA256 @($env:ASSET, "latest.yml") |
            ForEach-Object { "{0}  {1}" -f ($_.Hash.ToLower()), (Split-Path $_.Path -Leaf) }
          $hashLines | Set-Content SHA256SUMS.txt -Encoding utf8
          $hashBlock = $hashLines -join [Environment]::NewLine
          # 单引号 here-string：不插值（PowerShell 的 ` 在双引号里是转义符，markdown
          # 代码栅栏 ``` 会被吃掉）；占位符后置替换。
          #
          # 缩进纪律（2026-09-25 演练抓到的坑）：here-string 的**内容行与结束符在
          # YAML 里必须与其他行同缩进**——YAML 块标量会剥离共同缩进，PowerShell
          # 收到的仍是顶格文本（`'@` 顶格，5.1/7 都合规）。只给首尾两行缩进、
          # 内容行顶格时，那条顶格的 `---` 会被 YAML 当作文档分隔符，整份 workflow
          # 解析失败（push 产生 0 秒 failure、workflow_dispatch 报 422）。
          $notice = @'
          
          ---
          
          ## 安装说明（未签名软件 / unsigned build）
          
          本安装包**未经代码签名**。首次运行时 Windows 可能显示「Windows 已保护你的电脑」——
          这是未签名软件的正常提示：点「更多信息」→「仍要运行」。SmartScreen 信誉按版本重新
          积累，后续版本可能再次提示。
          
          This installer is **not code-signed**. On first run Windows may show "Windows protected
          your PC" — that is expected for unsigned software: click **More info** → **Run anyway**.
          SmartScreen reputation resets per release, so the notice may reappear on later versions.
          
          ## 校验和 / Checksums (SHA256)
          
          ```
          __HASHES__
          ```
          '@
          $notice = $notice.Replace("__HASHES__", $hashBlock)
          Add-Content -Path release-notes.md -Value $notice -Encoding utf8
          Write-Host "SHA256SUMS.txt 与安装说明已追加到 release-notes.md"

      # 安装/卸载冒烟（NSIS 静默模式）：GitHub runner 就是一台干净的 Windows——
      # 「装得上、卸得掉」这条最朴素的发布门禁在这里零成本覆盖。静默安装（/S）不需要
      # 交互桌面会话；「通知能弹出」这类需要真机桌面会话的项仍归人工清单
      # （docs/release-checklist.md）。
      - name: 安装与卸载冒烟（NSIS /S）
        shell: pwsh
        env:
          ASSET: ${{ steps.artifact.outputs.asset }}
        run: |
          $ErrorActionPreference = "Stop"
          $installer = Join-Path (Get-Location) $env:ASSET
          $proc = Start-Process -FilePath $installer -ArgumentList "/S" -Wait -PassThru
          if ($proc.ExitCode -ne 0) { throw "静默安装失败（exit $($proc.ExitCode)）" }

          # per-user 安装（perMachine: false）落在 %LOCALAPPDATA%\Programs 下；
          # 目录名跟着 productName（"Job Workbench"）走，探两个候选而不是写死。
          $candidates = @(
            (Join-Path $env:LOCALAPPDATA "Programs\job-workbench"),
            (Join-Path $env:LOCALAPPDATA "Programs\Job Workbench")
          )
          $installDir = $candidates | Where-Object { Test-Path (Join-Path $_ "Uninstall job-workbench.exe") } |
            Select-Object -First 1
          if (-not $installDir) {
            $listing = (Get-ChildItem (Join-Path $env:LOCALAPPDATA "Programs") -Directory -ErrorAction SilentlyContinue).Name -join ", "
            throw "装不上：%LOCALAPPDATA%\Programs 下找不到卸载器（现有目录：$listing）"
          }
          Write-Host "已安装到：$installDir"

          $uninstaller = Get-ChildItem $installDir -Filter "Uninstall*.exe" | Select-Object -First 1
          if (-not $uninstaller) { throw "安装目录里没有卸载器" }
          $uninst = Start-Process -FilePath $uninstaller.FullName -ArgumentList "/S" -Wait -PassThru
          if ($uninst.ExitCode -ne 0) { throw "静默卸载失败（exit $($uninst.ExitCode)）" }
          Write-Host "已卸载（安装 → 验证 → 卸载 全链路通过）"

      # 演练模式：只构建 + 校验 + 存成 workflow artifact，不碰 Release
      # 这是**唯一能在不打 tag 时验证"自动更新元数据有没有被正确产出"的入口**
      - name: 演练产物留存
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f  # v7.0.0
        with:
          name: installer
          path: |
            ${{ steps.artifact.outputs.asset }}
            ${{ steps.artifact.outputs.asset }}.blockmap
            latest.yml
            release-notes.md
            SHA256SUMS.txt
          retention-days: 7
          if-no-files-found: error

      # 正式发布也走 artifact 传给 publish job——写权限不能留在这一步
      - name: 传递产物给发布 job
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f  # v7.0.0
        with:
          name: release-payload
          path: |
            ${{ steps.artifact.outputs.asset }}
            ${{ steps.artifact.outputs.asset }}.blockmap
            latest.yml
            release-notes.md
            SHA256SUMS.txt
          retention-days: 3
          if-no-files-found: error

  publish:
    name: 创建 Release 并挂载安装包
    needs: build
    runs-on: windows-latest
    timeout-minutes: 20
    permissions:
      contents: write
    # 发布只能从 tag 发起。**不能**写成"手动触发且非 dry_run 就发布"：手动触发时
    # github.ref_name 是**分支名**（如 ci/release-automation），那样
    # `gh release create <分支名>` 会以分支名建出一个真的 tag 与 Release。
    # tag push 时 inputs.dry_run 为 null，`!= true` 成立，行为不变。
    if: startsWith(github.ref, 'refs/tags/') && inputs.dry_run != true
    steps:
      - uses: actions/download-artifact@70fc10c6e5e1ce46ad2ea6f2b72d43f7d47b13c3  # v8.0.0
        with:
          name: release-payload

      # 用 gh（runner 预装、第一方）而不是第三方 release action：少一个供应链依赖。
      # --clobber 让同一 tag 可以重跑；Release 已存在时只更新说明与资产，不重复创建。
      #
      # 每条 gh 后面都必须显式判 $LASTEXITCODE：GitHub 的 pwsh 包装器只在**脚本末尾**
      # 检查一次退出码，而 $ErrorActionPreference="Stop" 对原生命令不生效。漏判的后果
      # 很具体——`gh release upload` 失败后脚本继续跑，最后一条 `gh release view` 成功，
      # 于是步骤变绿而 Release 上根本没有安装包。那正好拆掉了"必须真的产出 exe"这道闸。
      - name: 创建或更新 Release 并上传资产
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
          # 这个 job 只下载 artifact、**没有 checkout**，而 gh 默认靠当前目录的 git 仓库
          # 推断目标仓库 → 会报 "failed to run git: fatal: not a git repository"。
          # 显式给 GH_REPO（gh 认这个环境变量），四条 gh 调用一次性全覆盖；
          # 不必为此引入一次代码拉取——publish 只需要 artifact 与说明文件。
          GH_REPO: ${{ github.repository }}
          TAG: ${{ github.ref_name }}
          ASSET: ${{ needs.build.outputs.asset }}
        run: |
          $ErrorActionPreference = "Stop"
          $asset = $env:ASSET
          if (-not (Test-Path $asset)) { throw "artifact 里没有找到 $asset" }

          # 这条失败属正常分支（Release 尚不存在），单独放宽，不看退出码
          $null = gh release view $env:TAG 2>$null
          if ($LASTEXITCODE -ne 0) {
            Write-Host "Release $($env:TAG) 不存在，创建"
            gh release create $env:TAG --title $env:TAG --notes-file release-notes.md
            if ($LASTEXITCODE -ne 0) { throw "创建 Release 失败（exit $LASTEXITCODE）" }
          } else {
            Write-Host "Release $($env:TAG) 已存在，更新说明"
            gh release edit $env:TAG --notes-file release-notes.md
            if ($LASTEXITCODE -ne 0) { throw "更新 Release 说明失败（exit $LASTEXITCODE）" }
          }

          # 安装包 + 自动更新元数据 + SHA256 清单一起传。latest.yml 缺了的话老用户
          # 永远收不到更新，而这件事在只检查 exe 的时候是**看不出来**的——都要核。
          $files = @($asset, "latest.yml", "SHA256SUMS.txt")
          if (Test-Path "$asset.blockmap") { $files += "$asset.blockmap" }
          gh release upload $env:TAG @files --clobber
          if ($LASTEXITCODE -ne 0) { throw "上传发布资产失败（exit $LASTEXITCODE）——发布未完成，不要当成成功" }

          # 上传之后再核一遍：以 Release 上真实存在的资产为准，而不是以退出码为依据
          $assets = gh release view $env:TAG --json assets --jq '.assets[].name'
          if ($LASTEXITCODE -ne 0) { throw "读取 Release 资产列表失败（exit $LASTEXITCODE）" }
          $missing = $files | Where-Object { $assets -notcontains $_ }
          if ($missing) {
            throw "上传命令返回 0，但 Release 资产列表里缺少：$($missing -join ', ')。实际列表：$($assets -join ', ')"
          }
          Write-Host "已发布：$($env:TAG) / $($files -join ', ')"
