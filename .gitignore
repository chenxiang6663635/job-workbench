# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# 临时与编辑器文件
*.tmp
*.bak
~$*
Thumbs.db
.DS_Store
.vscode/*
!.vscode/settings.json
.idea/

# Web 运行时产物：文件锁、依赖与构建输出
*.lock
node_modules/
dist/
build/
web/frontend/dist/
web/backend/dist/
web/backend/build/

# 简历 PDF、照片、预览图均为交付物，纳入版本管理，不在此忽略
# .codebuddy 目录：命令定义与技能文件纳入版本管理；
# 工作记忆（memory/，AI 跨会话笔记）、团队/自动化运行时产物、真实 MCP 配置除外

# 本地验证用的临时工作区（fake 数据），不进版本管理；personal 才是真实工作区
test_ws/
# README 截图用的 demo 数据工作区（假公司占位数据）；P2 将沉淀为 init_workspace.py --demo
demo-shots/

# 开发辅助工具的本地索引产物（运行时生成，clone 后跑 scripts/index_dev_tools.ps1 重建）
# 均可能含数十 MB 解析缓存，不进版本管理；另注意：这些目录索引的是本机个人工作区的代码，
# 别把包含 personal/ 的整份 .gitnexus/.codegraph 随意分享
.gitnexus/
.codegraph/
graphify-out/

# install_skills.py 分发到各 AI CLI 的 skill 副本（单一源在 skills/，.codebuddy/skills 随仓库）
# 以及 AI 计划工具的临时产物——都是运行生成，进 git 只会制造 untracked 噪音
.agents/
.claude/
.codex/
.codebuddy/plans/
# AI 工作记忆：跨会话笔记，个人向、频繁变更，不入库
.codebuddy/memory/
# 团队协作与定时任务的本地运行时状态
.codebuddy/teams/
.codebuddy/automations/
# 真实 MCP 配置可能含密钥；入库的是 mcp.example.json
.codebuddy/mcp.json

# 功能差距分析报告：一次性研究产物，结论已沉淀进 docs/specs/ 与 CHANGELOG
research_*.md

# pytest 运行缓存
.pytest_cache/
# 测试覆盖率产物
.coverage
htmlcov/
coverage/

# 原子写异常中断的残留临时文件（正常流程用完即删）
.jobws_tmp_*
# PDF 生成用临时 HTML（用完即删）
__preview_*.html

# 坏文件隔离区：损坏数据无入库价值，恢复靠快照备份
personal/05_投递追踪/quarantine/

# Electron 打包输出（electron-builder）
web/electron/release/
web/electron/out/

# 前端依赖安装与构建的本地日志
npm-debug.log*
yarn-error.log*

# 真实工作区整体不进版本管理（隐私护栏）
# 2026-09-07 决策：仓库要开源，personal/ 下的姓名 / 电话 / 邮箱 / 照片 / 投递记录 /
# 事实卡 / 岗位池全是真实数据，逐条忽略容易漏——整体忽略。
# 忽略只影响 git，本机照样编辑与生成（工作区发现只扫仓库根，所以必须留在原位）；
# 模板里的空样例在 template/ 下，不受影响。历史中的 personal/ 已用 filter-repo 清除。
personal/
