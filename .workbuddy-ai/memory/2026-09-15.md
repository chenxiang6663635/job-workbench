# 2026-09-15

## UI 六问诊断（只分析未改动）

用户反馈 6 项，逐一定位到代码（未执行修改）：

1. 弹窗"靠下"+ 想移动二级界面
   - 根因候选 A（主）：`web/frontend/src/components/ui/dialog.tsx` 的 `-translate-x-1/2 -translate-y-1/2`
     与 `data-[state=open]:animate-in zoom-in-95`（tailwindcss-animate 1.0.7）互斥——入场 200ms 内
     transform 被关键帧覆盖为 translate3d(0,0,0)，弹窗左上角落在视口中心。
   - 根因候选 B：`ImapFetchDialog` 用 `h-[82vh]` 固定高，观感重心偏低。
   - 已具备但弱：拖动只绑 `DialogHeader`（data-dialog-drag），无把手提示、offset 每次挂载重置、不可缩放。
2. 简历模板选择：标准版式只有硬编码的 `template/workspace/02_简历工坊/templates/std_resume.html`
   （`tools/resume_build.py:69 TEMPLATE_STD`），无版式选择器；手写 `resume_<版本>.html` 需用户自放。
   AI 只有逐条改写 `POST /api/resume/{version}/suggest`（resume_guard 五项护栏），无一键成稿。
3. 邮件实时链接：`tools/imap_fetch.py` 只返回 uid/subject/from/date/body，无 Message-ID / webmail 深链 /
   记录关联落库；tracker FIELDS 无邮件来源字段，STAGES 无"人才库"概念。
4. 设置外观：主题 11 套（含 system）+ 自定义编辑器；字族仅 2 套（Inter/System UI）；
   字号无独立档位，只有 Electron 全局缩放（zoom.js ZOOM_MIN/MAX/STEP = -3/3/0.5），浏览器端不可用。
5. tab 图标裁切：`ui/tabs.tsx` 已加 `[&_svg]:shrink-0`，但 `ui/segmented.tsx` 未加；
   顶栏 nav 的 `overflow-x-auto` 会让 overflow-y 计算为 auto（裁切发光/环）。
6. 测试文件堆积：Playwright 失败产物 `test-results/` `test-results-r2/` `test-results-r3/`
   （.gitignore 只写死 `web/frontend/test-results/`，带后缀的漏网 → git status 未跟踪）；
   另有 pytest 的 `tests/**/*.pyc`、根目录 51 个 `research_*.md` / `tmp_*` 草稿。
