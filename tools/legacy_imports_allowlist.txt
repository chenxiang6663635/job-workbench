# 旧名 import 点的**存量上限**（`jobws lint legacy-imports` 读它）。
#
# 形态与 `size_allowlist.txt` / `i18n_hardcode_allowlist.txt` 同款：上限即水位，
# **只许下降**——新增旧名 import 会当场报错，逼新代码走 `jobws_core`。
# 降到 0 就意味着 shim 可以删了（下一版）。
#
# 更新方式：先改代码把实测数降下来，再把上限改成新值。
# **不要**为了放过新增调用而调高上限——那等于把闸门焊死。
#
# 基线取自 2026-09-17 批 6（领域层包化，旧路径改 shim 的那一刻）：
# filelock / workspace_io：2026-09-19 A-4 已清零（18 处全改新名），两个 shim 文件
# 同时删除——它们的行随之从本清单移除，这就是「降到 0 即可删 shim」那条流程走完的样子。

# `pathres`：2026-09-19 PR-B 已清零（原 7 处全改新名），旧路径 `web/backend/pathres.py`
# 随之删除——本行也已移除，与 `filelock` / `workspace_io` 同款。名字留在
# `check_legacy_imports.LEGACY_NAMES` 里当防火墙（缺行即上限 0，谁再写就报错）。

# 2026-09-19 批 6 第二批（PR-A 搬 tracker/approval，PR-B 搬其余；ast 实测）：
#
# 这两个的最终水位**不是「没改完」**，而是**改不动**——剩下的调用点在两类位置上，
# 改新名会破坏被测对象：
#   ① **跨层测试**：`test_tracker_facade.py` 专测旧名门面契约（含留仓的 `_cli*`）；
#      `test_talks.py` / `test_mails.py` / `test_cli_surface.py` 走 CLI 侧（`_cli*`
#      只存在于留仓的 `tools/tracker/`）；`test_approval*.py` / `test_workspaces_api.py`
#      / `test_prep_toggle.py` 给**仓内** approval 的令牌目录打桩、断言它那张登记表。
#   ② **转发层自身与其入口**：`tools/jobws.py` 的 `track` 分发要的是留仓 CLI 的 `main`。
# 改新名会让它们测到包内实现、打桩打空——正是本项目反复记的那条教训：
# 「patch 要打到**调用点所在的模块**」。
tracker = 11  # 49 → 11：产品代码全部改新名，余下的是上面两类
approval = 9  # 26 → 9：8 处跨层/转发层 + **1 处后端 apply 入口**
              # （`web/backend/routers/approvals.py` 需要**完整**登记表——`prep.toggle`
              #  与 `init` 按设计留仓登记，包内只有十一个；用包内壳会让笔记勾选框写回
              #  得到 422，2026-09-19 的 UI 冒烟抓到过）
