# 旧名 import 点的**存量上限**（`jobws lint legacy-imports` 读它）。
#
# 形态与 `size_allowlist.txt` / `i18n_hardcode_allowlist.txt` 同款：上限即水位，
# **只许下降**——新增旧名 import 会当场报错，逼新代码走 `jobws_core`。
# 降到 0 就意味着 shim 可以删了（下一版）。
#
# 更新方式：先改代码把实测数降下来，再把上限改成新值。
# **不要**为了放过新增调用而调高上限——那等于把闸门焊死。
#
# 基线取自 2026-09-17 批 6（领域层包化，旧路径改 shim 的那一刻）：
filelock = 11  # 2026-09-19 A-2：tracker 的 5 处随搬迁改新名 → 后端 10 + 测试 1
workspace_io = 7  # 2026-09-19 A-2：tracker/_core 那处随搬迁改新名 → 后端 4 + prefs/report/测试

# 2026-09-19 批 6 第二批（pathres 搬包，ast 实测 7 处）：
#   后端 4（main / deps / routers/system 各 1）+ MCP 1（paths.py 注入点）
#   + 测试 2（conftest 注入点 / test_domain_root）
#   （CLI 那处走新名 `jobws_core.pathres`：jobws.py 是 __main__，旧名会真的打印
#     弃用告警，而 CLI 契约要求 stderr 干净）
pathres = 7
