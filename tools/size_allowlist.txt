# 规模预算的存量豁免清单（配合 tools/check_size.py 与 `jobws lint size`）
#
# 格式：`路径 = 登记行数  # 理由`。登记行数兼作**水位线**：
#   · 实际行数降到阈值以内 → 报错要求删掉这一行（自洁，防清单腐化）；
#   · 实际行数超过登记值 → 报错（存量只许变小，不许继续膨胀）；
#   · 在这两者之间 → 放行（存量债务，不还也别再借）。
#
# 本清单是**现状存档**，不是永久豁免：理由要写清"为什么这一项暂时不拆"，
# 拆完就删（留着等于给将来的同名文件预授权——i18n 豁免清单的教训）。
# 重新生成草稿：`python tools/check_size.py --print-allowlist`
#
# --- 领域层：拆分为重构专项，拆前只许变小 ---
# （2026-09-16 重构批：tracker 已包化为 tools/tracker/、progress 已包化为 routers/progress/，两行按自洁删除）
#
# --- 脚本：argparse 声明 + 处理流程，声明式占比大 ---
packages/jobws-core/src/jobws_core/jd_score.py = 558  # 评分标准 + 解析卡校验 + 差距分析（领域层）；2026-09-16 拆 main 为 _build_parser/_run_show_profile/_run_gap/_validate_card/_print_verdict（684→705）；2026-09-19 PR-B 从 tools/ 搬进领域包（705→707）；2026-09-21 H-2c 批 CLI 段迁 tools/_cli_jd_score.py，领域层不再 import argparse（707→558）
tools/resume_build.py = 558  # 模板渲染 + ATS 校验（领域函数；后端 routers/resume.py 直接调用）；2026-09-16 拆 main 与 cmd_render 为 helper（680→717）；2026-09-21 H-2d 批 CLI 段迁 tools/_cli_resume.py，VERIFY_FACTS_FILE 全局赋值改显式传参（717→558）
tools/check_i18n_hardcode.py = 663  # i18n 检查器（含 EN_SCOPE_RELS 与豁免解析）
packages/jobws-core/src/jobws_core/question_bank.py = 595  # 题库领域层 + 导入解析 + CSV 导入导出；2026-09-18 命令层拆到 _cli_bank.py（588→468）+ 更新预览显式列出自动写入的「最近复习」+ 合并改动提成 _merge_changes（468→475）；2026-09-19 PR-B 搬进领域包并去掉 sys.path 段（475→472）；2026-09-19 CSV 批加 _read_import_csv / preview_import_csv / export_csv（472→595，拆分列入后续）
packages/jobws-core/src/jobws_core/report.py = 535  # 复盘统计与聚类；2026-09-16 拆 build_report 为六节 helper（532→538）；2026-09-19 PR-B 搬进领域包并去掉 sys.path 段（538→535）
tools/init_workspace.py = 425  # 骨架复制 + demo 数据铺设
packages/jobws-core/src/jobws_core/status_parse.py = 329  # 阶段同义词映射与解析（2026-09-19 PR-B 从 tools/ 搬进领域包，水位随行）
# （2026-09-16 重构批：check_skills / install_skills 的长函数已拆，两行按自洁删除）
#
# --- 单域 router：端点 + 校验 + pydantic 模型 ---
web/backend/routers/applications.py = 572  # 时间线索引化（P 批 2026-09-21，+4）：history_by_id 建索引一次、逐行传子集
web/backend/routers/resume.py = 614  # 版式/风格清单 + 预览/Word/生成三处渲染
web/backend/routers/jobs.py = 502  # 2026-09-25 收口批：create_job 改原子写（+3，四端复核跟进；原水位 499）
web/backend/routers/imap.py = 304  # IMAP 配置 / 测试连接 / 只读拉取集中一处；本次加降级提示后越过 300 行
web/backend/routers/system.py = 332  # 2026-09-25 收口批：app_version() 去私有化 + docstring（+4；原水位 328——备份/导出/快照淘汰加微秒级暂存名 +2）
# （2026-09-16 重构批：dashboard() 已拆为五个 section helper，本行按自洁删除）
#
# --- 前端：刻意集中的单一真源 / 页面级容器 ---
web/frontend/src/api.ts = 460  # 前端唯一 API 客户端（端点清单 + 工作区状态再导出）；2026-09-17~09-19 多批加端点与类型（1174→1265）；2026-09-21 B-2 迁 previewQuestionUpdate 到 lib/bank.ts（1265→1258）；2026-09-21 H-1 批 requestJson / humanizeError / 工作区状态迁 lib/http.ts，四处副本合一（1258→1183）；2026-09-21 H-2a 批类型 / 枚举区（73 类型 + 13 常量）整体外移 lib/domainTypes.ts（1183→460）
web/frontend/src/pages/Applications.tsx = 461  # 页面容器（筛选 + 工具栏 + 新建表单 + 弹窗编排）；2026-09-17 关联邮件块 + 页头 + 空态原语 + 偏移同步（811→818）；2026-09-21 H-2b 表格拆 ApplicationsTable / ApplicationRow，元数据迁 lib/applicationMeta.ts（818→461）
# Settings.tsx 原 352 行登记：2026-09-26 收口批把「界面大小」卡拆到 settings/ZoomCard.tsx
# （352→232，跌破 300 阈值）→ 按自洁规则删掉这一行。
web/frontend/src/i18n/locales/zh-CN.ts = 1509  # 双语键集的源语言单一真源（en.ts 的编译期键集对齐约束依赖它，结构上不可随手拆文件）；2026-09-26 提醒条批加 7 键 = +9 行（注释 1 + 键 7 + 分隔空行 1，1500→1509，**零余量**——再加任一键即红）。减债方向：按域拆命名空间 / 合并同值键——列入 i18n 结构批
web/frontend/src/pages/Dashboard.tsx = 694  # 看板（统计卡 + 漏斗 + 待推进 + 复盘）；2026-09-17 页头 + 两处空态原语（698→700）；2026-09-21 登记值归零到实测（700→694，H-2 分叉②；只下调水位登记，未改代码）
web/frontend/src/pages/Resume.tsx = 544  # 简历工坊（模式切换 + 表单 + 预览 + 谱系）；2026-09-17 页头（两处 return）+ 空态原语（539→544）
web/frontend/src/pages/Jobs.tsx = 524  # 岗位池（列表 + 详情双栏）；2026-09-17 页头 + 空态原语（520→524）
web/frontend/src/components/StatusUpdateDialog.tsx = 559  # 解析 → 建议 → 确认三段式
web/frontend/src/components/MailList.tsx = 475  # 邮件列表 + 8 字段表单；2026-09-17 空态改 EmptyState 原语（469→475）

web/frontend/src/components/ResumeImportDialog.tsx = 301  # 导入核对页（逐段确认）
# ThemeEditor.tsx 原 307 行登记：2026-09-22 UX-5 把已存主题列表与删除确认拆出
# 为 ThemePresetList.tsx（307→233，跌破 300 阈值）→ 按自洁规则删掉这一行。

web/frontend/src/lib/theme.ts = 317  # 主题注册表 + 自定义主题 + 导入导出；2026-09-17 加 renameCustomTheme / exportThemeVarsAsCss 后越过 300（字体 / 字号已拆出到 fonts.ts；拆分列入后续重构）

# --- MCP 包与 scripts/：2026-09-23 二轮审计把这两处纳入扫描范围后暴露的存量 ---
# （此前 `SCAN_DIRS` 不含它们，MCP 包能无限膨胀而闸门一直报 OK——同一条理由当初
#  也适用于 packages/，那边早就纳入了。这几行是"现状存档"，只许变小。）
mcp/jobws_mcp/server.py = 358  # 工具注册表 + 参数模型；build_server() 301 行（按能力分组的注册块，拆分列入后续重构）
mcp/jobws_mcp/tools_readonly.py = 429  # 六个只读工具；dashboard_summary() 109 行（编排 + 组装，拆分列入后续重构）
scripts/smoke_backend_exe.py = 162  # 起 exe → 打健康检查 → 断言；main() 97 行（步骤线性，拆分列入后续重构）
#
# --- Electron 主进程：2026-09-25 发布前收口批把它与 .js/.mjs 纳入扫描 ---
# （此前 web/electron 与 .js 都在范围外；这一行是现状存档，只许变小。拆分
#  【composition root + backend_process / updater / window / navigation /
#  notifications】列入发布后重构批——发布窗口内不动刚修过两次的生命周期代码。）
web/electron/main.js = 967

# --- 邮件事实解析：2026-09-25 PR-4 新增归属传播（_resolve_target 抽出 +
#  extract_facts 恰好命中一条时向全部事实传播 targetId）——与 match_rows /
#  阶段信号强内聚，拆分收益低；待与「建议卡就地选定」同批稳定后再审视。）
packages/jobws-core/src/jobws_core/mail_facts.py = 317
