# 界面 token 一致性检查的允许清单（`tools/check_ui_tokens.py` 读它）。
#
# 格式与 `i18n_hardcode_allowlist.txt` 相同：一行一个文件，`=` 后面是**放行的片段**
# （用 | 分隔），行尾 `#` 是理由。**不设整文件放行**。
#
# 只登记「确有正当理由、且无法改成 token」的写法。当前为空——本批把 21 处写死的
# white/black 透明度都收敛成了 `--highlight` / `--scrim`，检查零豁免通过。
# 两个已知的**故意不拦**的形态写在这里备查（不需要登记，规则本身就不判）：
#   1. bare `bg-white`：A4 预览纸张与 PDF iframe 的「纸张是白的」物理隐喻
#      （2026-09-17 后白起点渐变文字已全部改实色，from-white 不再是合法形态）；
#   2. 尺寸类任意值（`max-h-[32rem]`）：属视觉刻度问题，由设计审计处理。
#
# 清单是「现状存档」：片段改掉或文件删除后必须同步删，否则脚本报
# 「片段已不再出现 / 文件已无命中」，避免给将来的同名写法预授权。
