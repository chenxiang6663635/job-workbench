# 方案：岗位评分放宽 + 简历宽母版

> 日期：2026-09-24｜状态：**待你决定后执行**｜本轮只出文档
> 你已定的范围：评分规则 ①②③ **全改**；简历 = **宽母版 + 现有两份同步重改**；先出方案、不动手。

## 0. 本轮不改动清单（白纸黑字）

本文件是**方案**，不是改动。以下一律未动：

- 未改 `skills/jwb-jd/SKILL.md`、`skills/jwb-recruit-coach/SKILL.md` 及其 `references/`
- 未改 `packages/jobws-core/src/jobws_core/jd_score.py`（阈值、门槛词、三态映射）
- 未改 `personal/AGENTS.md`、`personal/config/directions/*`（未新增方向文件）
- 未改简历三份 md、两份 HTML、两份 JSON，未重出任何 PDF
- 未改前端（`badgeVariants.ts` / `JobDetailView.tsx`）
- 未新建任何测试

## 1. 现状实证（决定改法的六条事实）

| # | 事实 | 证据 |
|---|---|---|
| 1 | 分数**只做展示/排序/聚合**，无任何过滤或置灰 | `web/backend/routers/jobs.py:221-258` 仅排序；`web/frontend/src/pages/Jobs.tsx:475-508` 渲染无过滤；`components/badgeVariants.ts:11-17` 是档位名→颜色的字符串包含（不解析数字）；看板高分口径派生自 `jd_score.THRESHOLDS`（`routers/dashboard.py:29-45`） |
| 2 | **硬门槛一票否决 + 终止流程** | `skills/jwb-jd/SKILL.md:38`「任一项 fail → 解析卡只填「基本信息」与「硬门槛」两节，写明不通过原因，**终止流程**。不打分、不生成材料」；`skills/jwb-recruit-coach/SKILL.md:35` 同义 |
| 3 | **`[待填]` 一律按 fail 处理** | `skills/jwb-jd/SKILL.md:29`、`skills/jwb-recruit-coach/SKILL.md:39`；配套 `:36` / `:39`「禁止猜测，禁止按「可能满足」放行」 |
| 4 | **城市 pass/fail 且不参与加权** | `skills/jwb-jd/SKILL.md:40`、`skills/jwb-recruit-coach/SKILL.md:41` |
| 5 | **方向契合 30 分只认锚点表**，当前只有 2 个方向 | `jd_score.py:316-366` `resolve_profile` 读 `<工作区>/config/directions/*.md`；现有 `personal/config/directions/hvac.md`、`datacenter.md` |
| 6 | 阈值与权重**纯硬编码**、无申诉通道、**硬门槛判定零测试** | `jd_score.py:32-37`（四维度 30/25/30/15）、`:39-46`（五档阈值）；`tools/prefs.py:39` 白名单只有 theme/font/resume_style；`tests/` 内 `## 硬门槛` 仅作夹具文本（`tests/test_job_linking.py:76-79`），**没有任何 gate 结论断言** |

**关键推论**：你感觉"评分太限制岗位"——限制**不在分数**，而在第 2/3/4 条（判定节奏与一票否决）和第 5 条（方向表窄导致跨方向岗位在 30 分那格结构性吃亏）。所以改法不是调阈值，而是**改判定节奏 + 补方向数据**。

另一条强论据：你档案里的现实边界是「城市跟随岗位无硬限制」（见长期记忆；**落地前需回读 `personal/AGENTS.md` 原文复核**）。若属实，则"城市一律 pass/fail"是**规则与事实不符**，把它当一票否决没有依据。

## 2. 岗位评分放宽方案

### 2.1 ① `[待填]` 不再判死 → 新增结论态「待补档案」

**改法**：门槛结论从两态扩为三态 —— `通过` / `不通过` / `待补档案`。

- `[待填]` → 结论写 `待补档案`，**不终止流程**；
- 解析卡「硬门槛」节增一行 `缺失字段: 学历、外语`（列出实际缺的项）；
- 解析卡「结论」节写：`补齐档案后可继续评分`，**仍不打分、不生成材料**；
- 只有**明确 fail 项**才写入「不通过原因」并终止。

**为什么不破红线**：红线禁的是"猜测放行"（`skills/jwb-jd/SKILL.md:36`）。「待补档案」不是放行，是把**一次判定拆成两步**：既不放行也不判死，判定权仍在你补完档案之后。

**涉及文件**：`skills/jwb-jd/SKILL.md`（2. Eligibility Gate 节）、`skills/jwb-recruit-coach/SKILL.md`（Eligibility Gate 节）、`jd_score.py:113-114`（`GATE_PASS_WORDS`/`GATE_FAIL_WORDS`）、`jd_score.py:182-192`（`_classify_gate`）、`web/frontend/src/components/badgeVariants.ts:36-40`（三态配色）。

注意：`_classify_gate` 现在把"非通过/非不通过"一律归为「待确认」——**必须显式区分**「待补档案」与「待确认」，否则界面上两者同色，用户不知道"是我要补档案"还是"规则读不懂"。建议三态三色。

### 2.2 ② 城市拆两级

**改法**：把城市要求分两类，写进两个 SKILL.md 的城市条款 + `skills/jwb-recruit-coach/references/scoring.md`：

- **硬性不可行**（要求本地户籍、必须长期驻外、明确不接受异地、倒班驻场）→ 维持 fail，终止；
- **偏好类**（"本地优先""周边亦可""可接受搬家"）→ 不判 fail，改为在「培养与稳定性」扣分，档位建议 `-3`（轻度偏好）或 `-5`（较强偏好），并在结论里写明差距句。

**上限约束**：「培养与稳定性」满分 15（`jd_score.py:36`），扣分须设单项上限（建议 **≤5**），避免城市偏好吃掉整个维度。四档判据写在 `skills/jwb-recruit-coach/references/scoring.md:16-23`（13–15 / 9–12 / 5–8 / 0–4）——扣分后落在哪一档要能对上，否则会出现"扣了分但判据不自洽"。

### 2.3 ③ 方向锚点表扩容（2 → 6）

**改法**：在 `personal/config/directions/` 新增方向文件（**纯数据，零代码改动**）。格式照现有 `directions/*.md`（补充词 + 方向锚点表，见 `docs/domain-contract.md:81-84`）。

| 新方向文件 | 覆盖口径（建议锚点词条方向） |
|---|---|
| `thermal-management.md` | 热管理：电子/芯片热管理、冷板液冷、CDU、热界面、均温、热阻网络 |
| `thermal-fluid-cfd.md` | 热流体仿真/CFD：湍流模型、边界条件、网格无关性、场分析（温度/速度/湿度/CO₂） |
| `thermal-design-cae.md` | 热设计/CAE：结构-热耦合、散热结构、可靠性、仿真-实验对标 |
| `datacenter.md`（已有，补充） | 补液冷/冷板/CDU/PUE/容量匹配/N+1 等词条 |
| `hvac.md`（已有，补充） | 补洁净室、工艺空调、冷源优化等词条 |
| `energy-storage-thermal.md` | 储能/电池热管理：PACK 液冷与风冷、均温、热失控边界（**仅列你事实库支持的词条**） |

**边界说明**：`docs/domain-contract.md:106-107` 把"**新的评分维度**"划为插件边界外——本方案**不动维度**，只是加方向数据文件，属插件边界内，无需改代码。

**依赖**：新增方向后，解析卡的「方向」字段与 CLI `--direction` 取值要能选用（`tools/_cli_jd_score.py:26-39`）；Web 侧 `GET /api/jobs/{id}/gap` 的 `direction` 参数同理（`routers/jobs.py:469-499`）。

### 2.4 附带：让「待补档案」可推进

`JobDetailView.tsx:52-84` 已渲染硬门槛的结论徽章 / 原因 / 字段 / 逐条依据；`routers/jobs.py:128` 已解析 `hardGates` → **响应契约不用改**，只需前端对「待补档案」态多渲染一行"缺哪些字段 + 去哪补"（指向 `personal/AGENTS.md` 的「硬门槛事实」节）。

### 2.5 验收与测试（当前是盲区）

现在**没有任何测试**盯硬门槛判定（事实 6）。改规则必须同时补网，否则改坏无感：

1. `[待填]` → 结论 = `待补档案`、**不终止**、不产出分数与材料；
2. 明确 fail → 仍终止、仍写「不通过原因」；
3. 城市偏好类 → 不 fail，且「培养与稳定性」扣分 ≤ 上限、扣完落档与判据自洽；
4. `_classify_gate` 对三态词的判定（`jd_score.py:182-192`）；
5. `gateBadgeVariant` 三态配色（`badgeVariants.ts:36-40`）。

### 2.6 明确不动

- **不动权重**（技术 30 / 经历 25 / 方向 30 / 培养 15）与**五档阈值**（`jd_score.py:32-46`）：它们是硬编码、无配置入口，改了会让**历史岗位档位集体重算**，与本方案的目标（放宽判定）无关。
- **不动诚实红线**：不编造、不猜测放行（`skills/jwb-recruit-coach/SKILL.md:52-57`）。
- **不做**"误判申诉/人工覆盖"端点（现有手段只有手改解析卡；这属另一个议题，本方案不引入）。

## 3. 简历改造方案

> **范围界定（2026-09-24 用户明确）**：本节的**内容判断与定稿权归你本人**，**不纳入开发批次**。
> 开发流程只负责工具与文档侧（评分规则、上手路径、以及既有 `jobws resume render` 这条已有能力）。
> 若你希望把"四层同步"之类的动作也脚本化，那属于**新开发需求**，需另行立项。

### 3.1 先定真源（**这是必须先拍板的一件事**）

现状四层载体，且**链路不同源**：

| 层 | 文件 | 被谁消费 |
|---|---|---|
| md×3 | `personal/02_简历工坊/简历_主版_v1.0.md`、`简历_HVAC空调制冷版_v1.0.md`、`简历_数据中心冷却版_v1.0.md` | 人读 / 叙述镜像 |
| HTML×2 | `personal/02_简历工坊/pdf/resume_hvac.html`（125 行）、`resume_datacenter.html`（126 行）——**手写，无占位符** | **PDF 生成走这一层**（`tools/resume_build.py:478-493` 扫 `pdf/resume_*.html`） |
| JSON×2 | `personal/02_简历工坊/source/resume_hvac.json`、`resume_datacenter.json` | **差距分析走这一层**（`jd_score.py:452-477` 读 `source/resume_<版本>.json`） |
| PDF×2 | `谌翔_简历_空调制冷HVAC_v1.5色块版.pdf`、`谌翔_简历_数据中心冷却_v1.5色块版.pdf` | 投递用 |

**风险**：若只改 HTML，`jobws jd --gap` 仍按旧 JSON 算差距；若只改 JSON，PDF 不变。二者都会让"你看的和系统算的不一致"。
**建议**：以 **md 为内容真源**（人改最舒服），改完同步 JSON + HTML；并在本批结束前跑一次交叉核对（3.6）。若你更愿意以 HTML 为唯一真源，则需要一个"HTML → JSON 导出"的小工具，属额外工作量，需另评估。

### 3.2 宽母版的目标结构

| 段 | 宽母版写法（要点） | 与现状比 |
|---|---|---|
| 顶部定位行 | `求职方向：热管理 · 热流体仿真 · 暖通制冷系统` | 「空调制冷 / HVAC 系统工程 · 能效优化与运行控制」→ 放宽；**不整行删** |
| 专业概况 | 一句话同时覆盖传热/流体/暖通制冷/热管理 + HVAC 控制 + EnergyPlus/CFD + Python + AI 辅助工作流 | 2 行 → 1 行，解绑"occupancy + sensor robustness" |
| 项目一 | **2 bullet**：①问题+方法（占用→分区新风需求与 DOAS 阀位、CCS/OBCS 对比工况）②结果+工程判断（总 HVAC 用电 −52.1%（DOAS −69.9%、VRF −9.3%）、PPD 14.3%→7.2%、CO₂ 峰值近 1200 ppm 的节能-IAQ 权衡） | 3 → 2 |
| 项目二 | **2 bullet**：①问题与实验（Python + XGBoost、完整/测试端缺失/缺失感知训练多场景、5 种缺失策略、10%–50% 缺失率、多次随机重复）②结果与价值（缺失致性能下降、训练期接触缺失在部分条件下正向补偿、影响取决于变量/策略/严重度） | 3 → 2 |
| 工程实践 | 1 条（冷机 → 冷冻水 → 末端 → 冷却水 → 冷却塔 全链路，用"热流体系统实践"语言）+ 可选 1 条（空间布置 / 设备连接 / 可维护性约束） | 保留、**换角度不换事实** |
| 科研成果 | 1 行（项目标题已含"SCI 二区 TOP · 第二作者"→ 此处只留论文题目） | 去重复 |
| 技能 | **4 行**：热流体/HVAC ｜ AI 辅助科研工作流 ｜ 数据与仿真（Python / XGBoost / EnergyPlus / CFD / Origin、Matplotlib）｜ 工程工具（AutoCAD / 天正） | 5 → 4 |
| 奖项 / CET | 不动 | — |

**不整行删顶部定位行的理由**：ATS 场景下这一行是**关键词位**；改成"能力口径"既保留关键词密度，又不把自己锁死。定向版再把它换成具体方向即可。

### 3.3 定向版只允许改四处（既存规则，不要新发明）

`skills/jwb-recruit-coach/SKILL.md:65` 原文：「多版本简历 = 1 个事实母版 + N 个派生，**只改四处**：求职方向、Profile、技能顺序、项目表述。项目事实一律不动。」

### 3.4 两版的现状差异实测（共 5 处）

| 差异点 | HVAC 版 | 数据中心版 |
|---|---|---|
| 顶部定位行 `:69` | 空调制冷 / HVAC 系统工程 · 能效优化与运行控制 | 数据中心冷却 / 热管理 · 暖通与制冷系统 |
| 专业概况 `:73` | 侧重风水系统与 HVAC 控制 | 增"系统学习数据中心空气冷却、冷冻水与液冷设施架构" |
| 项目一 bullet1 首词 `:86` | 控制策略 | 控制与运行 |
| 工程实践首句 `:101` | 施工检查 + 现场接触设备清单 | 结合水系统原理图核对冷源/水系统/空气侧管线 |
| 技能行序 `:112` | 暖通/制冷 起头 | "数据中心冷却基础"起头 |

→ 定向版就是在这 5 处替换；母版才承担结构瘦身。

### 3.5 硬约束（不能破）

1. **ATS 三项**：页数 = 1、可提取文本 ≥ 300 字符、`config/ats_required_facts.txt` 逐词命中（`tools/resume_build.py:508-551`）。→ **压字前先把 `ats_required_facts.txt` 清单打印出来逐条比对**，确认要删的句子不承载必需关键词。
2. **四层同步**（md×2 / JSON×2 / HTML×2）；先例见 `personal/AGENTS.md:131`（v1.5 就是"四层同步 + 重出 PDF"）。
3. **版本号**：md 文件名沿用 `_v1.0` 编号以免全库断链（`personal/AGENTS.md:95`）；PDF 命名建议 `谌翔_简历_<方向>_v1.6色块版.pdf`（**版本号待你定**）。
4. **诚实红线**：所有表述必须在 `00_事实库/` 有出处（`skills/jwb-recruit-coach/SKILL.md:16`）；项目事实一律不动；不写"独立从零建模"这类超出事实的话。
5. **生成命令纠偏**：重出 PDF 用 `python tools/jobws.py resume render`（实现 = `tools/resume_build.py`，Chrome/Edge headless `--print-to-pdf`）。**`personal/99_归档/build_resume_pdf.ps1` 已归档、不再维护**（`resume_build.py:4-6` 明说取代它）——长期记忆里那条记法已过期，本轮已修正。

### 3.6 压字目标与三步自检

- **目标**：项目段 3→2 bullet（两处）、技能 5→4 行、概况 2→1 行、科研成果去重 1 处；总量约 **−20%**（你说 20–30% 是合理区间，但受 ATS 关键词约束，先按 −20% 试，不够再压）。
- **自检**：① `jobws jd --gap --resume <版本>` 对比 `matched / injectable / missing` 变化（确认没丢已覆盖词）；② `jobws resume render` 过 ATS 三项；③ 人眼看一页排版是否仍有空余与呼吸感。

## 4. 落地顺序（你决定后）

1. **先做评分放宽**（2.1 → 2.2 → 2.3，配 2.5 测试）：它决定"接下来投不投"，时效性最高；方向锚点表是纯数据，风险最低。
2. **再做简历**：定真源 → 改宽母版 → 派生两份定向版 → 四层同步 → 重出 PDF → ATS 校验。
3. **最后回测**：拿新规则把现有岗位池重跑一遍，统计**有多少岗位从"终止"变成"可评估"、有多少跨方向岗位的「方向契合」分发生变化**——这是"放宽是否真的有效"的唯一硬证据。

## 5. 待你拍板的三件事

1. **简历真源定在哪一层**（md / HTML / JSON）？——建议 md，改完脚本化同步 JSON 与 HTML。
2. **新版本号**用 v1.6 还是别的？
3. **城市偏好类扣分**取 `-3` 还是 `-5`？以及「培养与稳定性」单项扣分上限是否就定 5 分？

## 6. 别人怎么用：上手路径与文档缺口（2026-09-24 补）

### 6.1 已有的：面向使用者的文档比想象中厚

| 文档 | 定位 | 面向 |
|---|---|---|
| `README.md`（151 行）/ `README.zh-CN.md`（133 行） | 入口：定位 / 三层架构 / 功能一览 / 8 张截图 / Quick start 三步 / 下载 / 文档索引 | 普通使用者（含只想下 exe 的人） |
| `docs/usage-guide.zh-CN.md`（417 行）/ `usage-guide.md`（416 行） | 完整手册，八章：环境准备 / 三种启动方式 / 八页面 / AI 工作流 / CLI 速查 / 数据与备份 / FAQ（9 小节）/ 技术文档导航 | 普通使用者 |
| `docs/README.md`（107 行） | 文档总索引，按阅读顺序排列 | 所有人 |
| `docs/support-and-compatibility.md`（63 行） | "用户会问的三个问题"（毕业条件 / 支持环境 / 数据兼容） | 使用者 |
| 桌面安装包说明 | `usage-guide.zh-CN.md:106-120`：向导式、`/S` 静默安装、`/D=<目录>`、数据在 `%APPDATA%\job-workbench`、重建用 `scripts\build_desktop.ps1` | 非技术用户 |
| demo 一条命令试看 | `README.md:65-68`：`jobws init --target demo --demo`（8 投递 / 3 面试 / 2 联系人 / 1 Offer / 3 宣讲会 / 6 题，全占位数据） | 想先看效果的人 |
| 截图 | `docs/screenshots/` 共 16 张（8 页 × 中英），`npm run capture` 生成 | — |
| 第三条路：不克隆 | `README.md:83-94`（插件市场 / `npx skills add`） | 非技术用户 |
| 产品内首启引导 | `components/OnboardingWizard.tsx`（三步：命名 → 选领域 → 确认，落盘走一次性令牌）+ `EmptyOnboarding` 首页空态两个 CTA（新建工作区 / 体验示例数据） | — |

**所以"有没有技术文档"这个问题，答案是：有，而且分了三层**（用户手册 / 贡献者契约 `docs/domain-contract.md` / 决策记录 `docs/decisions/`）。

### 6.2 缺口：新用户真正会卡的地方（带证据）

| # | 缺口 | 证据 | 后果 |
|---|---|---|---|
| 1 | **"填档案"没有文档小节** | 手册里 `AGENTS.md` 只出现 3 次（`usage-guide.zh-CN.md:255`/`:330`/`:353`），**没有任何"怎么填"**；填法只在 `README.md:77-80` 一行 + init 之后才出现的 `template/AGENTS.example.md:32-43` | **与第 2 节①是同一件事的两面**：档案没填 → `[待填]` → 现在按 fail **判死岗位** |
| 2 | **`skills install` 只有 CLI，手册安装章节没写** | Quick start 第 2 步就是它（`README.md:73-75`）；但 `usage-guide.zh-CN.md:11-50` 完全没提、只在 `:402` 导航表一句；Web 八页无入口（`App.tsx:26-35` TABS 仅 8 项） | 装 exe 直接用 Web 的用户**从没分发过技能** → AI 工作流（jwb-jd / jwb-apply）在他机器上根本不存在 |
| 3 | **"我该选哪个领域插件"无使用者指引** | 只有 `docs/README.md:83-84` 报名 + 产品内一句 `zh-CN.ts:544`「拿不准就先用默认的那个」；`init` 未指定时只列 ID、不给适用人群（`init_workspace.py:367-368`）；而两份 `profile.md:7-9` 的「适用人群 / 覆盖岗位」**要 init 之后才进工作区** | 鸡生蛋：**init 时就得选，可依据却在 init 之后才出现** |
| 4 | 三处 init 例子不一致 | `README.md:71` 用 `software-backend`、`usage-guide.zh-CN.md:38` 用 `hvac-cooling`、`template/workspace/README.md:3` 不带 `--domain` | 用户不知道哪个"是对的" |
| 5 | **五个核心概念无使用者向解释** | 工作区 / 领域插件 / 解析卡 / 硬门槛事实 / 事实库 在 `docs/glossary.md`（37 条，维护者向）里**全无**；唯一解释散在 UI 空态文案（`zh-CN.ts:533`/`:544`）与 init 后的工作区 README | 看手册能操作，但不知道自己在操作什么 |
| 6 | **前端没有任何帮助入口** | 导航 8 项无 help（`App.tsx:26-35`）；「关于」卡只有版本 + 平台（`Settings.tsx:591-623`）；全前端唯一外链是 Provider 推广（`:429`） | 用户卡住时，界面里**没有一条通向 `docs/` 的路** |
| 7 | 无视频、无安装向导截图 | 搜 `视频 / video / tutorial / 录屏` 全仓零命中；`docs/screenshots/` 只有页面截图 | 非技术用户仍需读文字 |
| 8 | `web/frontend/README.md` 仍是 Vite 模板原文 | 50 行，`:1`「# React + TypeScript + Vite」 | 想改前端的人第一眼看不出这是什么项目 |

### 6.3 建议补什么（按"最小成本、最大解除卡点"排序）

1. **手册增一节「填档案（AGENTS.md）」**：六项硬门槛事实怎么填（照 `AGENTS.example.md:32-43`）、第五节自定义红线、以及"为什么必填"（不填 → 岗位无法评估）。成本 = 文档一节；收益 = 与第 2 节①合成闭环（既放宽判定，又告诉你怎么补）。
2. **安装章节补上 `skills install`，并写明"用桌面安装包的用户也要跑一次"**；更彻底的做法是桌面端给一个"分发技能"按钮。成本 = 几行文档（＋可选一个设置项）。
3. **把"选哪个领域插件"写成使用者可见的一张表**：直接把两份 `profile.md:7-9` 的「适用人群 / 覆盖岗位 / 内置方向」搬进 `docs/README.md` 的领域插件小节（或新开 `docs/choosing-a-domain.md`），并让 `init` 未指定 `--domain` 时**打印这张表**而非只列 ID。成本低（内容已写好，只是换位置 + 改一句输出）。
4. **统一三处 init 例子**，另一处改成"换成你专业的插件"的说明。
5. **加一份使用者向「概念速查」**（5 个概念，每个 2–3 句），或并进手册第一节之后；`docs/glossary.md` 保持维护者向不动。
6. **前端加帮助入口**：设置页「关于」卡加一行指向 `docs/usage-guide.zh-CN.md`；空态里的"去填档案"CTA 指向对应文档。
7. 可选：安装向导 / 首次运行截图 2–3 张；`web/frontend/README.md` 改写。

### 6.4 一条重要判断

**CLI 路径其实已经能上手**（三步 + demo 试看 + 手册八章），**桌面端更顺**（有 onboarding 引导）。真正拦人的不是"文档太少"，而是**两处断点**：

1. **填档案没有文档，而它是评分的前置条件** → 与第 2 节①是同一件事：**既放宽判定，也补"怎么填"**；
2. **技能分发只有 CLI 入口** → Web / exe 用户拿不到 AI 工作流。

结论：让别人轻易上手**不需要重写文档体系**，只需补这两处断点 + 一张选插件表 + 界面里一个帮助入口。

### 6.5 待拍板（接第 5 节）

**第 4 条：6.3 里那 7 项要不要纳入本批？** 我的建议是至少纳入 **1 / 2 / 3 / 6** 四项（成本低、且直接解除上面两个断点）；5 / 7 可放后续批次；4 顺手做。

### 6.6 四端引导面盘点：有没有"弹出技术文档引导"？（2026-09-24 再补）

**结论：基本没有。** 四端分布极不均 —— MCP 最好、CLI 次之、插件有"必读"强制、**桌面端与 Web 为零**。关键要区分两件事：**"下一步引导"其实很多**（空态文案、init 三条、离线屏、MCP 的 `next_step`），**缺的是"卡住时去哪儿看文档"的入口** —— 目前唯一通向 `docs/` 的路是 GitHub 仓库页或本地文件管理器。

| 端 | 已有的引导 | 指向 `docs/` 的入口 | 缺口 |
|---|---|---|---|
| **CLI** | 成功后的"下一步"很齐：`init_workspace.py:115-118`（填档案硬门槛 / 填红线 / 建事实卡）+ `:120-124`（指向工作区 README）；`_cli_jd_score.py:131-132`（档位 + 下一步）；`track add`（归档目录建议）；`_cli_resume.py:110-112`（ATS 未过"不要归档投递"）；13 个旧脚本的统一迁移提示 | **仅 2 处**：`check_domains.py:190`「贡献契约见 docs/domain-contract.md」、`check_four_ends.py:206,208`（four-ends 再生成） | **`--help` 零文档指向**（`jobws.py:158-159` epilog 只说"接 --help"）——而这是新人第一条命令 |
| **MCP** | **四端最全**：`mcp/README.md` 有安装 / 宿主配置 / 故障排查，且 `:135` 指向 `docs/mcp-integration.md`；工具描述带操作引导（`server.py:99-100`「先把 diff 展示给用户…不要跳过」）；所有 `preview_*` 返回 `next_step`（`tools_writable.py:31-32`）；文档以 `jobws://` resource + prompt 形态暴露 | 有（README → `docs/mcp-integration.md`） | 工具描述只指工具名/资源 URI/技能名，不指仓库 docs |
| **插件** | **"必读"是强制的**：`skills/jwb-jd/SKILL.md:13`「执行前必读：工作区 `AGENTS.md` 与 `jwb-recruit-coach`…**不读这两处不得开始评分**」；`jwb-apply:14` 同款；`references/*.md` 交叉引用多处（`jwb-jd:65,90`、`jwb-track:51`、`jwb-recruit-coach:45`）；`commands/*.md` 有可复制示例 +「参数细节以 `jobws xxx --help` 为准」 | 不指仓库 docs（只指工作区文档与技能） | 对"评分正确性"够用，对"新人上手"不够 |
| **桌面端** | 只有**异常弹窗**：后端启动超时 / 找不到 Python / 后端起不来 / 运行中死亡 / 更新失败（`web/electron/main.js:252,292,319,323,710`；文案 `web/electron/i18n.js:34-45,66-79`）；到点提醒 Notification（`:502-518`） | **零** | 无自定义菜单（`Menu.buildFromTemplate` 未找到，且 `:614` `setMenuBarVisibility(false)` 隐藏菜单栏）、`shell.openExternal` 全仓仅 1 处且只服务页面外链（`:641`）、**无欢迎页 / 无首次运行检测**、无托盘（`:456-461` 明确"不建托盘"）、preload 仅 6 个偏好方法（`preload.js:12-36`，无 openDocs / openHelp） |
| **Web** | **内联"下一步"丰富**：8 条空态引导（`zh-CN.ts:93-94` 评分由 AI 在 jd 工作流完成、`:435-436` 缺「下一步动作」、`:492` 空岗位池、`:533` 工作区是什么、`:201` 题库、`:188` 抽题队列…）+ 离线屏有可复制启动命令与重试（`App.tsx:197-214`） | **零** | 导航 8 项无 help（`App.tsx:26-35`）；「关于」卡只有版本 + 平台（`Settings.tsx:591-629`）；所有 `target="_blank"` 都是用户数据驱动（会议链接 / 邮件 / 笔记外链 / 宣讲会 / Provider 推广 `:428-431`）；源码里 `usage-guide` 与指向 docs 的字符串**零命中** |

**由此归纳出四条最低成本的补法（并入 6.3 的清单）：**

8. **`jobws --help` 的 epilog 加一行文档指引**（如"使用手册 `docs/usage-guide.zh-CN.md`；领域插件契约 `docs/domain-contract.md`"）。成本 = 一行文本；收益 = **新人第一条命令**就能拿到手册入口。
9. **Web 设置页「关于」卡加一个文档入口**。关键杠杆：**桌面端就是同一套前端，改 Web 一处 = 两端同时获得**（桌面端本身没有菜单栏可加，这是四端里性价比最高的一改）。
10. **Electron 的异常弹窗补一句排查指引**（后端超时 / 死亡 / 找不到 Python）。成本 = i18n 两条文案；收益 = 用户在最容易放弃的那一刻看到去哪儿查。
11. **顺手清一个死 key**：`zh-CN.ts:527-529` 的 `dash.emptyTitle` / `dash.emptyHint` 无任何引用（`Dashboard.tsx` 走的是 `EmptyOnboarding` 分支）。

## 7. 后续流程总览：开发流程 vs 你自己的简历优化

> 依据：2026-09-24 用户要求"**简历优化不放入开发流程**"。下面把两件事彻底分开：左边是**我能执行的开发批次**，右边是**只有你能做决定的简历定稿**。

### 7.1 开发流程（按依赖排序，共四条线）

| 线 | 内容 | 状态 | 依赖 / 阻塞 |
|---|---|---|---|
| **① 收口批合并** | PR #182（收口批六项，7/7 检查已过） | **待你合并** | 合并后，任何堆叠分支的 base 要从 `feat/pre-release-hardening` 改回 `main` |
| **② 邮箱与模型配置**（计划 `feat/mail-and-model-setup`，11 待办） | A 邮箱：`mail_providers.py` 预设表 + `imap_session.py`（含 IMAP `ID` 命令，修 163/126 连不上）→ `routers/mail_setup.py` → `ImapCard.tsx`（服务商下拉 / 授权码引导 / 文件夹候选 / 错误分级）<br>B 模型：provider 加 `model` 落盘 + `/models` 结构化 → `ProviderCard.tsx`（服务商预设 / 模型可点选 / base_url 校验）+ 三处使用点共用<br>C/D：正文三态标注 + 深链降级说明<br>E：OAuth2 可行性调研（只出文档） | 计划 ready，**未开工** | 开工前先定分支（建议堆叠在 #182 上） |
| **③ 评分放宽 + 上手路径**（本方案第 2 节 + 6.3/6.6） | 评分：`[待填]` 不再判死 →「待补档案」；城市拆两级；方向锚点表 2→6；配 5 条测试<br>上手：手册增「填档案」一节；安装章节补 `skills install`；把两份 `profile.md` 的适用人群表搬成使用者可见 + 让 `init` 打印它；设置页「关于」卡加文档入口<br>引导：`jobws --help` epilog 加文档指引；Electron 异常弹窗补排查指引；清死 key `dash.empty*` | 方案已出，**待你拍板后转计划** | 需先拍板第 5 节四项（真源 / 版本号 / 城市扣分 / 6.3 那 7 项范围） |
| **④ 文档余项（低优先）** | 使用者向概念速查（工作区 / 领域插件 / 解析卡 / 硬门槛事实 / 事实库）；安装向导与首次运行截图 2–3 张；`web/frontend/README.md` 改写（现为 Vite 模板原文） | 候选池 | 无 |

**两条硬纪律不变**：不自动 commit / 不自动合并；每笔本地门禁全绿才推进（`pytest` + `skills check` + 七个扫描器；涉前端补 `tsc -b` / `eslint` / 前端单测 / 相关 Playwright）。

### 7.2 你自己的简历优化（不占开发批次）

| # | 你要做的决定 / 动作 | 为什么只能你做 |
|---|---|---|
| 1 | 定**真源层**（md / HTML / JSON）与新版本号 | 决定后续所有同步动作的方向 |
| 2 | 定**顶部定位行**的口径（能力口径如"热管理 · 热流体仿真 · 暖通制冷系统"，还是具体方向） | 这是你对外自我定义的第一句 |
| 3 | 项目一、项目二各**压成哪两条**（问题+方法 / 结果+判断） | 事实表述的取舍；红线是每句在 `00_事实库/` 有出处 |
| 4 | 实习段**改写角度**（从"施工检查"转"热流体系统实践"）的措辞确认 | 涉及"做过什么"的自我陈述边界 |
| 5 | 科研成果**去重方案**（A：项目标题不写"SCI 二区 TOP"；B：标题保留、成果只留一行） | 一页空间分配的价值判断 |
| 6 | **技能四行**的内容与顺序（母版口径） | 命中率与诚实的平衡 |
| 7 | 压字后的**人眼验收**（一页是否仍有空余、哪句必须留） | ATS 只能验页数与关键词，审美与分量要你判断 |
| 8 | 是否需要**额外定向版**（第三 / 第四个方向） | 取决于你下一阶段的投递面 |
| 9 | 是否要我**只跑既有工具**（`jobws resume render`：四层同步核对 → 重出 PDF → ATS 三项） | 这是执行动作、不是内容判断；需要时你说一声即可 |

### 7.3 一句话边界

**开发流程 = 工具与文档能让"你用得更顺"的部分（①②③④）；简历优化 = 内容该怎么写的判断（7.2）。**
两者唯一的交集是第 9 项——一个已经存在的命令，不需要新开发。

## 附：本方案与既有规则的关系（避免被当成"新发明"）

- 「宽母版 + 定向版」「只改四处」= `skills/jwb-recruit-coach/SKILL.md:65` **既有规则**，本方案只是把它执行到位。
- 「加方向属插件边界内」= `docs/domain-contract.md:81-84,106-107`。
- 「不打分、不生成材料」= 两处 SKILL.md 既有纪律，本方案**保留**（只改"何时终止"）。
- 与另一条在途计划的边界：邮箱/模型配置改造（`feat/mail-and-model-setup`）与本方案**无重叠文件**，可并行、互不阻塞。
