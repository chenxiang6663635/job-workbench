# 方案：系统级提醒完整版 + 长任务扩展（面试周 / 备考周）

> 2026-09-24。来源：用户拍板「顺带立项」两条 —— `ROADMAP.md:89`（系统级提醒）与 `ROADMAP.md:88`（长任务扩展）。
> 本文只做**立项与方案**，不改任何代码；行号基于 2026-09-24 实测，**开工前按纪律重跑 code-explorer 复验**。
> 前置：第 12 笔「邮件截止识别」已完成（截止日期会写进 `下次动作日期` + `下次动作`），所以本文两条都建立在"待办桶里已经有真实截止项"之上。

---

## 零、为什么是现在

`ROADMAP.md:89` 那条候选写明的触发条件是「桌面壳长期常驻，且**「错过截止日」真实发生过**」。用户 2026-09-24 提出的需求（"根据邮件里面倒计时增加过期 AI 面试或者笔试等提醒"）正是这个信号；同时最小形态的提醒（收口批笔 5）已经落地，**从"有提醒"到"提醒够用"的距离已经可以量出来了**——见第一节的五处缺口。

---

## 一、系统级提醒完整版

### 1.1 现状（实测，别重复排查）

| 环节 | 现状 | 位置 |
|---|---|---|
| 数据来源 | 复用看板三桶：近 7 天待办（`下次动作日期` **或** `截止日期` ∈ [today, today+7]）/ 近 7 天宣讲会 / 已过截止仍待投 | `routers/reminders.py:31-40`；口径 helper `routers/dashboard.py:108-169` |
| 通知条数 | 后端每桶给前 5 条（`LIMIT=5`）、主进程最多显示 3 行（`MAX_LINES=3`） | `reminders.py:28`、`electron/reminders.js:10` |
| 去重粒度 | **一天一次**：`state.lastNotified !== today` 才发 | `electron/reminders.js:26-29` |
| 检查频率 | 每 6 小时一次；后端未起时静默跳过；关窗即停 | `electron/main.js:470,520-544` |
| 通知点击 | 只 `win.show() + focus()`——**不带上下文，落不到那条记录** | `electron/main.js:509-512` |
| 已过截止 | **只覆盖 `当前阶段 == "待投"` 且 `截止日期 < today`** ——"AI 面/测评已过期没做"这类落不进任何桶 | `routers/dashboard.py:156-169` |
| 设置项 | 一个总开关（桌面端才有），无节奏配置 | `settingsRegistry.ts:75-80`、`usePreferenceEntries.ts:171-185` |

### 1.2 五处缺口（本批要解决的）

1. **没有"提前 N 天"的节奏**：现在只要落在 7 天窗口里就一次报完；真实需要的节奏是"距截止 3 天提醒一次 + 当天再提醒一次"。
2. **一天一次的粒度对一个待办太多的用户不够**：`lastNotified` 是**全局**日期标记——今天提醒过之后，**今天新出现的**（如刚写完的截止项）就再也不报了。
3. **已过截止的覆盖太窄**：只认 `待投` + `截止日期`；`下次动作日期` 已过（"该做没做"）不算。
4. **通知点击无落点**：用户收到"完成在线测评"却点不进去，得自己到追踪表里找。
5. **无静默/节奏配置**：不能选"提前几天"、不能临时静音某条。

### 1.3 改法（分层，尽量不动既有口径）

**后端（`routers/reminders.py` 扩展，口径仍与看板同源）**
- 响应从"三桶"扩为**带档位的条目**：每条 `{id, 公司, 岗位, date, kind, bucket, daysLeft, text}`，`bucket ∈ overdue / today / soon / talks`；
- 窗口参数化：默认 7 天，允许 `days` 查询参数（3/5/7）——**窗口仍由 `dashboard` 的 helper 决定，不另造一套**；
- 待办桶补一类：`下次动作日期` 已过且非终态 → 进 `overdue`（现在漏的正是这类）；
- 保持**只读**（不动 `tracker.lock`、不写任何文件）。

**Electron（`electron/reminders.js`）**
- 去重从"一天一次"改为**每事项每档一次**：`state.notified = { "<id>:<bucket>:<date>": <YYYY-MM-DD> }`，只对"该发的"去重；
- 状态文件加**清理**：只保留 `date >= today-1` 或未来条目的键（否则长年累月会涨）；
- 通知正文：每条带"还剩 N 天 / 今天到期 / 已过期 N 天"（文案进 `electron/i18n.js` 双语）；
- **通知点击带上下文**：`win.show()` 后通过既有 IPC/preload 通道（或 `webContents.send`）把 `{tab, focusId}` 交给前端，前端跳到追踪表并展开该记录；前端已有 query 参数下钻先例（看板的 `dueWithin` / `overdue`），优先复用同一机制而不是新造路由。

**设置（前端）**
- 「到点提醒」从开关扩为一组：**开关 + 提前几天（3/5/7，默认 3）+ 静音到明天**；仍只出现在桌面端（`settingsRegistry` 的 `desktopOnly` 语义照旧）；
- 新文案双语；`settingsRegistry` 的搜索词同步补（否则设置页搜索会漏）。

**候选池**
- `ROADMAP.md:89` 从候选池移出（移入 `Now` 段一行状态指针：已实现 / 进行中）。

### 1.4 测试面

- 后端：`tests/test_reminders_api.py` 扩档位分桶、`days` 参数、已过 `下次动作日期` 入桶、只读断言（既有三条是安全网）；
- Electron：`web/electron/reminders.test.js` 扩"每事项每档一次""跨日重置""状态清理""文案含剩余天数"（既有 5 组是安全网）；
- 前端单测：设置项与搜索词；
- e2e：桌面端无 E2E 先例（`reminders.test.js:2` 注释已说明），保持现状——**不为此新引入 Electron E2E 基建**。

### 1.5 明确不做

- **不做常驻后台 / 托盘 / 开机自启**（`main.js:456-461` 的"不建托盘"是有意设计，本批不动）；
- 不做手机推送（`ROADMAP.md:86` 的 390px 与手机场景仍在候选池）；
- 不做"按项目分组的静音规则"（只有"静音到明天"这一档）。

### 1.6 工作量与风险

约 **2–3 人日**（后端 1、Electron 1、设置与文案 0.5、测试 0.5）。最大风险在**通知点击定位**：它要求前端能"被外部参数定位到某条记录"，而当前追踪表的展开态是组件内 state——需先确认可否复用 query 参数机制（开工第一件事）。

---

## 二、长任务扩展（面试周 / 备考周）

### 2.1 现状

- 没有任何"排期 / 时间线"视图：全仓唯一的 Timeline 是 `HistoryTimeline.tsx`（**变更历史**，读 `history.csv`），不是日程；
- 唯一的时间面 = 看板的"近 7 天"三类区块（`Dashboard.tsx:546-612`）与 `InterviewList.tsx:49-53,181-190` 的**48 小时倒计时**（只作用于面试列表）；
- 可用的时间数据点分散在五张表：主表 `下次动作日期` / `截止日期`、面试表 `面试时间`、宣讲会 `时间`、Offer `答复截止日`、联系人 `下次跟进`（`_schema.py:20-21,71,92,146,157`）；
- `.ics` 导出只覆盖面试与宣讲会（`icsutil.py:130-201`）。

### 2.2 要做成什么（建议形态：**看板区块 → 独立页 的两步走**）

**第一步（建议先做，1–2 人日）**：看板新增「未来 14 天」区块
- 一个纯聚合只读端点（如 `GET /api/dashboard/schedule?days=14`）：把五张表的时间点统一成 `{date, kind, title, id, tab}`；
- 前端按天分组渲染（今天 / 明天 / 具体日期），每行可点到对应页面；与既有"近 7 天待办"区块**互补不重复**（后者是"要做的"，前者是"所有时间点"）；
- 空态一句话（"未来两周没有排期"）。

**第二步（视使用情况，2–3 人日）**：独立「排期」视图 + 按周复盘
- 左列日历 / 右列某天的条目；周切换；
- **按周复盘**复用既有复盘组件（`RetrospectivePanel`）的思路：读 `history.csv` 的变更时间线 + 状态变化，给出"这周推进了什么 / 卡在哪"；
- 导出：把当前视野的条目导成 `.ics`（复用 `icsutil.build_ics`），这样"面试周"能一次性进系统日历。

### 2.3 与候选池的关系

`ROADMAP.md:88`（长任务扩展）触发条件是「出现连续两周以上的高强度面试或备考」。**建议先确认这个条件是否已经成立**：若未成立，只做第一步（看板 14 天区块）即可，第二步留在候选池；成立则两步一起做。

### 2.4 明确不做

- 不做拖拽改期（写操作要两段式，拖拽会把确认流程打散）；
- 不做 RRULE / 时区增强（`mail_ics.py:9-13` 的已知边界保持）；
- 不做日历的双向同步（本地优先、不上云）。

---

## 三、建议顺序与依赖

```
① 本批收口        feat/mail-and-model-setup（邮箱 + 模型 + 正文口径 + OAuth 调研 + 邮件截止）
                  → 代码全部完成，只差提交与 PR（等用户放行）
② 提醒完整版      新分支（本方案第一节）——小而硬，2–3 人日；先解决"通知点击定位"的可行性
③ 长任务扩展      第一步（看板 14 天区块，1–2 人日）→ 用一段时间后再决定第二步
```

依赖关系：② 依赖 ①（截止项要先能写进 `下次动作日期`，完整版才有真实数据可提醒）；③ 与 ② 无重叠文件，可并行，但建议 ② 先做完再开 ③（避免同时改 settings 与 dashboard 两处登记面）。

---

## 四、待拍板

1. **提醒节奏**：提前几天？固定 3 天，还是做成可配（3/5/7）？（建议：可配，默认 3）
2. **通知点击定位**：纳入本批（需要前端支持"被外部定位到某条记录"），还是先只做"跳追踪表"这一档？
3. **已过截止扩展**：是否把"非终态 + `下次动作日期` 已过"也纳入过期提醒？（建议：纳入——这正是"错过截止日"的另一种形态）
4. **长任务扩展**：只做第一步（看板 14 天区块），还是两步一起？
5. **顺序**：② 与 ③ 是否按上面建议的顺序走？
